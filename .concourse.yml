platform: linux

image_resource:
  type: docker-image
  source:
    repository: java

inputs:
  - name: cf-demo
outputs: 
  - name: artifact-dir

run:
  dir: cf-demo
  path: ./mvnw
  args:
    - install 
      cp ./target/app.jar artifact-dir
